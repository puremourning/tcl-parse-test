{
  "configurations": {
    "Run": {
      "adapter": "CodeLLDB",
      "variables": {
        "make": {
          "shell": [ "python3", "-m", "compiledb", "make", "TARGET=debug" ]
        }
      },
      "configuration": {
        "request": "launch",
        "program": "${workspaceRoot}/debug/${fileBasenameNoExtension}",
        "args": [ "*${args}" ],
        "cwd": "${workspaceRoot}",
        "stopOnEntry#json": "${StopOnEntry:false}",
        "expressions": "native"
      }
    }
  }
}
